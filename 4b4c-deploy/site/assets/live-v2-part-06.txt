<div class="field"><label>Phase roadmap</label><select name="milestoneId"><option value="">Hors roadmap</option>${milestoneOptions(modal.projectId,modal.milestoneId)}</select></div>`:''}<div class="field"><label>Priorité</label><select name="priority"><option value="normal">Normale</option><option value="high">Haute</option><option value="urgent">Urgente</option><option value="low">Basse</option></select></div><div class="field"><label>Échéance</label><input name="dueAt" type="datetime-local"></div><div class="field span-2"><label>Description</label><textarea name="description"></textarea></div></div><div class="modal-actions"><button class="btn" type="button" data-action="close-modal">Annuler</button><button class="btn primary" type="submit">Créer</button></div></form>`);
  if (modal.type==='request') return modalFrame('Nouvelle demande','Adressez une demande explicite à une personne, avec un état de réponse.',`<form data-form="request"><div class="form-grid"><div class="field span-2"><label>Titre</label><input name="title" required autofocus></div><div class="field"><label>Projet</label><select name="projectId"><option value="">Sans projet</option>${projectOptions(modal.projectId,false)}</select></div><div class="field"><label>Destinataire</label><select name="recipient" required>${memberOptions(state.user.id)}</select></div><div class="field span-2"><label>Demande</label><textarea name="body"></textarea></div><div class="field"><label>Échéance</label><input name="dueAt" type="datetime-local"></div></div><div class="modal-actions"><button class="btn" type="button" data-action="close-modal">Annuler</button><button class="btn primary" type="submit">Envoyer la demande</button></div></form>`);
  if (modal.type==='decision') return modalFrame('Consigner une décision','Gardez le choix et sa justification dans la mémoire du projet.',`<form data-form="decision"><input type="hidden" name="projectId" value="${escAttr(modal.projectId)}"><div class="stack"><div class="field"><label>Décision</label><input name="title" required autofocus></div><div class="field"><label>Pourquoi ?</label><textarea name="rationale"></textarea></div></div><div class="modal-actions"><button class="btn" type="button" data-action="close-modal">Annuler</button><button class="btn primary" type="submit">Enregistrer</button></div></form>`);
  if (modal.type==='milestone') return modalFrame('Nouvelle phase','Une étape lisible de la roadmap, avec responsable et fenêtre temporelle.',`<form data-form="milestone"><input type="hidden" name="projectId" value="${escAttr(modal.projectId)}"><div class="form-grid"><div class="field span-2"><label>Titre</label><input name="title" required autofocus></div><div class="field span-2"><label>Résultat attendu</label><textarea name="description" placeholder="Qu’est-ce qui doit être vrai pour considérer cette phase terminée ?"></textarea></div><div class="field"><label>Responsable</label><select name="ownerId"><option value="">Non assigné</option>${memberOptions()}</select></div><div class="field"><label>État</label><select name="status"><option value="todo">À venir</option><option value="active">En cours</option><option value="done">Terminée</option></select></div><div class="field"><label>Début</label><input name="startDate" type="date"></div><div class="field"><label>Date cible</label><input name="dueDate" type="date"></div></div><div class="modal-actions"><button class="btn" type="button" data-action="close-modal">Annuler</button><button class="btn primary" type="submit">Créer la phase</button></div></form>`);
  if (modal.type==='milestone-edit') { const m=(state.projectCache.get(modal.projectId)?.milestones||[]).find(x=>x.id===modal.milestoneId); if(!m)return ''; return modalFrame('Modifier la phase','Mettez à jour la roadmap sans perdre l’historique des actions.',`<form data-form="milestone-edit"><input type="hidden" name="projectId" value="${escAttr(modal.projectId)}"><input type="hidden" name="milestoneId" value="${escAttr(m.id)}"><div class="form-grid"><div class="field span-2"><label>Titre</label><input name="title" required value="${escAttr(m.title)}"></div><div class="field span-2"><label>Résultat attendu</label><textarea name="description">${esc(m.description||'')}</textarea></div><div class="field"><label>Responsable</label><select name="ownerId"><option value="">Non assigné</option>${memberOptionsSelected(m.owner_id)}</select></div><div class="field"><label>État</label><select name="status">${['todo','active','done','cancelled'].map(v=>`<option value="${v}" ${m.status===v?'selected':''}>${milestoneLabel(v)}</option>`).join('')}</select></div><div class="field"><label>Début</label><input name="startDate" type="date" value="${escAttr(m.start_date||'')}"></div><div class="field"><label>Date cible</label><input name="dueDate" type="date" value="${escAttr(m.due_date||'')}"></div></div><div class="modal-actions"><button class="btn" type="button" data-action="close-modal">Annuler</button><button class="btn primary" type="submit">Enregistrer</button></div></form>`); }
  if (modal.type==='meeting') return modalFrame('Planifier une réunion','La réunion reste rattachée au projet et à son historique.',`<form data-form="meeting"><div class="form-grid"><div class="field span-2"><label>Titre</label><input name="title" required autofocus></div><div class="field"><label>Projet</label><select name="projectId"><option value="">Réunion d’espace</option>${projectOptions(modal.projectId,false)}</select></div><div class="field"><label>Début</label><input name="startsAt" type="datetime-local" required></div><div class="field"><label>Fin</label><input name="endsAt" type="datetime-local"></div><div class="field"><label>Salon visio (optionnel)</label><input name="videoRoom" placeholder="Nom du salon"></div></div><div class="modal-actions"><button class="btn" type="button" data-action="close-modal">Annuler</button><button class="btn primary" type="submit">Planifier</button></div></form>`);
  if (modal.type==='upload') return modalFrame('Ajouter une ressource','Fichier privé, visible selon les droits du projet.',`<form data-form="upload"><input type="hidden" name="projectId" value="${escAttr(modal.projectId)}"><div class="stack"><div class="field"><label>Titre</label><input name="title" required autofocus></div><div class="field"><label>Visibilité</label><select name="visibility"><option value="internal">Interne</option><option value="shared">Partageable avec invités du projet</option></select></div><div class="field"><label>Fichier (50 Mo max)</label><input name="file" type="file" required></div></div><div class="modal-actions"><button class="btn" type="button" data-action="close-modal">Annuler</button><button class="btn primary" type="submit">Importer</button></div></form>`);
  if (modal.type==='invite') return modalFrame('Inviter un membre','Le lien est lié à l’adresse email et expire dans 14 jours.',`<form data-form="invite"><div class="form-