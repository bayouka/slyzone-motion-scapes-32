unction renderSettings() {
  const avatar = avatarHtml(state.user.id);
  return `<div class="section-head"><div><span class="eyebrow">Configuration</span><h2>Paramètres</h2><p>Votre profil, l’espace de travail et l’état du pilote.</p></div></div>
    <div class="grid cols-2">
      <div class="card"><div class="card-head"><h2>Mon profil</h2><button class="btn small" data-action="edit-profile">Modifier</button></div>
        <div class="profile-summary">${avatar}<div><strong>${esc(displayName(state.user.id))}</strong><small>${esc(state.user.email||'')}</small></div></div>
        <div class="metric-label" style="margin-top:12px">Le nom et l’avatar sont visibles par les membres qui partagent un espace avec vous.</div>
      </div>
      <div class="card"><div class="card-head"><h2>Espace</h2></div><div class="stack"><div class="list-row"><div class="list-main"><small>Nom</small><strong>${esc(state.workspace.name)}</strong></div></div><div class="list-row"><div class="list-main"><small>Votre rôle</small><strong>${workspaceRoleLabel(state.workspaceRole)}</strong></div></div><div class="list-row"><div class="list-main"><small>Backend</small><strong>Supabase · Paris eu-west-3</strong></div><span class="pill good">Connecté</span></div></div></div>
    </div>
    <div class="card" style="margin-top:16px"><div class="card-head"><div><h2>Pilote 4b4c</h2><div class="metric-label">Données réelles, droits RLS et stockage partagé.</div></div><button class="btn small icon-only" data-action="refresh" title="Vérifier la synchronisation">↻</button></div><div class="notice">Cet espace est le pilote réel. Les fonctions expérimentales ou de démonstration ne sont pas affichées dans l’interface normale.</div></div>`;
}

function renderNotificationPanel() {
  const items = state.notifications.slice(0,12);
  return `<div class="notification-panel"><div class="card-head"><h3>Notifications</h3>${state.notifications.some(n=>!n.read_at)?`<button class="btn small" data-action="read-all-notifications">Tout lire</button>`:''}</div>${items.length?`<div class="stack">${items.map(n=>`<div class="list-row clickable" data-action="open-notification" data-id="${n.id}" data-route="${escAttr(n.route||'#/dashboard')}"><span class="dot" style="color:${n.read_at?'#c8ced8':'#3867f4'}"></span><div class="list-main"><strong>${esc(n.title)}</strong><small>${notificationKind(n.kind)} · ${relativeDate(new Date(n.created_at))}</small></div></div>`).join('')}</div>`:empty('Aucune notification','Les nouvelles affectations et discussions apparaîtront ici.')}</div>`;
}

function renderModal(modal) {
  if (modal.type==='quick-add') return modalFrame('Créer','Choisissez le type d’objet à créer.',`<div class="grid cols-2"><button class="btn" data-action="new-project">◫ Projet</button><button class="btn" data-action="new-action">✓ Action</button><button class="btn" data-action="new-request">? Demande</button><button class="btn" data-action="new-meeting">□ Réunion</button></div>`);
  if (modal.type==='project') return modalFrame('Nouveau projet','Crée un projet réel, sa conversation générale et votre rôle de lead.',`<form data-form="project"><div class="stack"><div class="field"><label>Nom</label><input name="name" required maxlength="160" autofocus></div><div class="field"><label>Objectif</label><textarea name="objective" placeholder="Quel résultat doit produire ce projet ?"></textarea></div><div class="field"><label>Date cible</label><input name="targetDate" type="date"></div></div><div class="modal-actions"><button class="btn" type="button" data-action="close-modal">Annuler</button><button class="btn primary" type="submit">Créer le projet</button></div></form>`);
  if (modal.type==='project-edit') { const p=state.projects.find(x=>x.id===modal.projectId); if(!p)return ''; return modalFrame('Modifier le projet','Nom, objectif, état, santé et date cible.',`<form data-form="project-edit"><input type="hidden" name="projectId" value="${p.id}"><div class="form-grid"><div class="field span-2"><label>Nom</label><input name="name" required maxlength="160" value="${escAttr(p.name)}"></div><div class="field span-2"><label>Objectif</label><textarea name="objective">${esc(p.objective||'')}</textarea></div><div class="field"><label>État</label><select name="status">${['active','on_hold','completed'].map(v=>`<option value="${v}" ${p.status===v?'selected':''}>${projectStatusLabel(v)}</option>`).join('')}</select></div><div class="field"><label>Santé</label><select name="health">${['on_track','at_risk','off_track'].map(v=>`<option value="${v}" ${p.health===v?'selected':''}>${healthLabel(v)}</option>`).join('')}</select></div><div class="field"><label>Date cible</label><input name="targetDate" type="date" value="${escAttr(p.target_date||'')}"></div></div><div class="modal-actions"><button class="btn" type="button" data-action="close-modal">Annuler</button><button class="btn primary" type="submit">Enregistrer</button></div></form>`); }
  if (modal.type==='project-manage') { const p=state.projects.find(x=>x.id===modal.projectId); if(!p)return ''; return modalFrame('Gérer le projet','Archivez de préférence. La suppression définitive est réservée aux projets sans ressource stockée.',`<div class="notice"><strong>${esc(p.name)}</strong><br>L’archivage masque le projet sans perdre son historique.</div><div class="modal-actions"><button class="btn" data-action="close-modal">Annuler</button><button class="btn" data-action="archive-project" data-project="${p.id}">Archiver</button><button class="btn danger" data-action="delete-project" data-project="${p.id}">Supprimer définitivement</button></div>`); }
  if (modal.type==='profile') return modalFrame('Modifier mon profil','Votre nom affiché et votre avatar seront visibles par votre équipe.',`<form data-form="profile"><div class="stack"><div class="profile-editor">${avatarHtml(state.user.id)}<div class="field" style="flex:1"><label>Nom affiché</label><input name="displayName" required maxlength="80" value="${escAttr(displayName(state.user.id))}"></div></div><div class="field"><label>Avatar</label><input name="avatar" type="file" accept="image/jpeg,image/png,image/webp,image/gif"><small>JPG, PNG, WebP ou GIF · 5 Mo max.</small></div></div><div class="modal-actions"><button class="btn" type="button" data-action="close-modal">Annuler</button><button class="btn primary" type="submit">Enregistrer</button></div></form>`);
  if (modal.type==='action') return modalFrame('Nouvelle action','Une unité de travail assignable, datée et traçable.',`<form data-form="action"><div class="form-grid"><div class="field span-2"><label>Titre</label><input name="title" required maxlength="200" autofocus></div><div class="field"><label>Projet</label><select name="projectId" required>${projectOptions(modal.projectId)}</select></div><div class="field"><label>Responsable</label><select name="assignee"><option value="">Non assigné</option>${memberOptions()}</select></div>${modal.projectId?`